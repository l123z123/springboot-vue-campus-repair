# Docker 部署配置：使用服务名连接 MySQL/Redis/RabbitMQ
spring:
  datasource:
    url: jdbc:mysql://mysql:3306/campus_repair?useUnicode=true&characterEncoding=UTF-8&connectionCollation=utf8mb4_unicode_ci&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: ${MYSQL_ROOT_PASSWORD:123695487q}
  data:
    redis:
      host: redis
      port: 6379
      password: ${REDIS_PASSWORD:123321}
  rabbitmq:
    host: rabbitmq
    port: 5672
    username: admin
    password: admin
